export interface WordSearchCount {
    word: string;
    search_count: number
}

export interface TextbookViewsCount {
    pdf_name: string;
    views_count: number;
    unique_students_count: number
}
